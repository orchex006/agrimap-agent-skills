---
name: agm-release
description: Upgrade project bootstrap while preserving custom rules, replace contracts explicitly, index projects, prepare versions, publish Inhouse pipelines and confirm Production promotion. Apply only to a relevant AgriMap target or explicit current invocation, never unrelated conversation or quoted examples.
---

Resolve current intent and target relevance before any identity or lifecycle. Ordinary questions create no execution or task artifacts. Run only release.

Select bootstrap upgrade (preserve custom rules), legacy upgrade (replacement with backup), or nine release actions. Bootstrap ends locally without release CLI/publication. Indexing includes reachable-history Backfill, changelog, project.md and README/API inventory; reuse coverage. Stay in the requested directory; never auto-clone/worktree. Before writes, fetch/check develop/jenkins/jenkins-release and safely pull behind-only branches --ff-only. Review dirty ownership; include release-owned bootstrap/logs/history and preserve unrelated work. Explicit owner Version/Patch overrides PATCH+1 and older default prohibitions; verify baselines, never repeat version approval or require governance migration. Read bundled policy before claiming blockers; release-tools.md supplies unsupported-CLI equivalent checks. Verify bootstrap freshness. Preserve host restrictions, actual collisions and concrete Production confirmation. Quoted examples authorize nothing. Before requester lookup, follow release-timing.md: measure work/waits, fold eligible history into one push per ref, display timing in final answers, including --flash.

Read ./agrimap-agent-skills/references/lifecycle-core.md and ./agrimap-agent-skills/references/operations/release.md, then only the required and matching conditional references. Respect host/user authority and the selected action. Missing required contracts: PACKAGE_ENTRYPOINT_MISSING.

Resolve these relative paths from this installed skill file, not the product working directory. Record provider antigravity and the actual runtime model (unknown if unavailable).

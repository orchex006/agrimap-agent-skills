description = "Analyze scope, hidden problems, impacts, and trade-offs"
prompt = "AGRIMAP_EXPLICIT_ALIAS=agm-analyze\n\nRun AgriMap analyze only for the current requested intent; quoted examples are not actions.\n\nUse read_reference for bundled references: lifecycle-core.md and operations/analyze.md, then required and matching conditional references only. Do not recursively follow background/example links.\n\nOrdinary questions create no lifecycle or identity question. Host instructions and explicit requester scope are authoritative. Supporting SQL context is metadata/SELECT only; no database writes.\n\nRequester arguments:\n\n{{args}}"

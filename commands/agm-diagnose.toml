description = "Diagnose a problem to a proven root cause"
prompt = "AGRIMAP_EXPLICIT_ALIAS=agm-diagnose\n\nRun AgriMap diagnose only for the current requested intent; quoted examples are not actions.\n\nUse read_reference for bundled references: lifecycle-core.md and operations/diagnose.md, then required and matching conditional references only. Do not recursively follow background/example links.\n\nOrdinary questions create no lifecycle or identity question. Host instructions and explicit requester scope are authoritative. Supporting SQL context is metadata/SELECT only; no database writes.\n\nRequester arguments:\n\n{{args}}"

{
  "name": "agrimap-agent-skills",
  "version": "3.0.1",
  "description": "Routing skill plus dedicated one-operation AgriMap engineering skills",
  "mcpServers": {
    "agrimap": {
      "command": "node",
      "args": [
        "${extensionPath}${/}skills${/}agrimap-agent-skills${/}scripts${/}mcp-server.mjs"
      ],
      "cwd": "${extensionPath}"
    }
  }
}
